name: file operatioons in github without checkout

on: workflow_dispatch

jobs:
    files:
        runs-on: ubuntu-latest
        steps:
            -   name: display the content of a file
                run:  cat f1.txt
            -   name: display the date
                run: date

